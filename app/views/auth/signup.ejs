<!DOCTYPE html>
<html lang="fr">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BlablaBook</title>
  <link rel="stylesheet" href="/style.css">
</head>

<body>
  <header class="header">
    <h1>BLABLABOOK</h1>
    <!--barre de recherche au centre du header -->
    <!--<form action="/search" method="GET">  -->

    <div class="nav-search">
      <form action="#" method="GET">
        <input type="text" name="query" placeholder="Rechercher un livre" aria-label="Champ de recherche">
        <button type="submit" aria-label="lancer la recherche">🔍</button>
      </form>
    </div>
    <!-- section bouton -->
    <div class="nav-buttons">
      <a href="/" class="nav-link" aria-label="Retour à la page d'accueil">Accueil</a>
      <a href="/" class="nav-link" aria-label="Explorer la bibliothèque numérique">Bibliothèque</a>
      <a href="/" class="nav-link" aria-label="Connectez-vous pour accéder à votre espace personnel">Connexi
        <a href="/" class="nav-link" aria-label="Consultez votre profil utilisateur">Utilisateur</a>
    </div>

  </header>
  <h1>Inscription</h1> <!-- Toast en cas d'erreur -->
  <% if (typeof errors !=="undefined" ) { %>
    <div class="alert alert-danger" role="alert">
      <% for (const errorAttribute in errors) { %> <%- errors[errorAttribute] %> <br>
          <% } %>
    </div>
    <% } %>
      <main class="register-form">
        <h2>Inscription</h2>
        <form action="/creer-un-compte" method="POST">
          <label for="firstname">Prénom</label>
          <input required type="text" id="firstname" name="firstname" placeholder="Prénom" value="<%= typeof data !== 'undefined' && data.firstname ? data.firstname : '' %>">
        
          <label for="lastname">Nom</label>
          <input type="text" id="lastname" name="lastname" placeholder="Nom" value="<%= typeof data !== 'undefined' && data.lastname ? data.lastname : '' %>">
        
          <label for="email">Email</label>
          <input type="email" id="email" name="email" placeholder="email@domain.com" value="<%= typeof data !== 'undefined' && data.email ? data.email : '' %>">
        
          <label for="password">Mot de passe</label>
          <input type="password" id="password" name="password" placeholder="*********">
        
          <label for="confirmation">Confirmation du mot de passe</label>
          <input type="password" id="confirmation" name="confirmation" placeholder="*********">
        
          <input type="submit" value="S'inscrire">
        </form> 
        <footer class="footer">
          <p>&copy; 2025 BlablaBook - Prototype réalisé par Farida</p>
        </footer>
</body>

</html>